#include<stdi.h>
int main() {
    char ch;
    printf("enter any character:\n");
    scanf("%c", &ch);
    if(ch=='a' || ch=='e' || ch=='i' || ch=='0' || ch=="u" || ch=='A' ch=='E' || ch=='I' ||ch=='O' || ch=='U')
    printf("\n is a vowel", ch);
    else
    printf("\n is not a vowel" ch);
    return 0;
}